# ContractLens backend — copy to .env and fill in your keys.
# Required for full document analysis and insights (MiniMax):
MINIMAX_API_KEY=your-minimax-api-key

# Optional: Student Privacy Agent uses AWS Bedrock. If not set, agent uses built-in fallback replies.
# On AWS Lambda, the function role is used instead of these.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
